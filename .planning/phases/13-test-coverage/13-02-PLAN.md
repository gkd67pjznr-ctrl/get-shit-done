---
phase: 13-test-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e.test.cjs
autonomous: true
requirements:
  - TEST-06

must_haves:
  truths:
    - "Legacy mode init plan-phase returns layout_style 'legacy' and planning_root ending in /.planning"
    - "Legacy mode init execute-phase returns layout_style 'legacy' and state_path starting with .planning/"
    - "Milestone-scoped mode init plan-phase with --milestone returns layout_style 'milestone-scoped' and planning_root containing milestones/v2.0"
    - "Milestone-scoped mode init execute-phase with --milestone returns milestone workspace paths"
    - "Milestone-scoped phase complete with --milestone updates milestone workspace ROADMAP.md (not root)"
  artifacts:
    - path: "tests/e2e.test.cjs"
      provides: "End-to-end test for plan-execute-verify in both layout modes"
      min_lines: 100
  key_links:
    - from: "tests/e2e.test.cjs"
      to: "get-shit-done/bin/gsd-tools.cjs"
      via: "runGsdToolsFull CLI invocation"
      pattern: "runGsdToolsFull"
    - from: "tests/e2e.test.cjs"
      to: "get-shit-done/bin/lib/commands.cjs"
      via: "init plan-phase, init execute-phase, phase complete commands"
      pattern: "init.*plan-phase|init.*execute-phase|phase.*complete"
---

<objective>
Create end-to-end test file covering the full plan-execute-verify lifecycle in both legacy and milestone-scoped layout modes.

Purpose: Satisfy TEST-06 requirement — end-to-end test executing plan, execute, verify flows in both layout modes. This validates that the entire CLI routing chain works correctly: flag parsing, command dispatch, path resolution, file system operations, and output serialization.

Output: New `tests/e2e.test.cjs` file with two describe blocks (legacy mode, milestone-scoped mode), each testing init plan-phase, init execute-phase, and phase complete commands.
</objective>

<execution_context>
@/Users/tmac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tmac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-test-coverage/13-RESEARCH.md

<interfaces>
<!-- Existing helpers used for E2E test scaffolding -->

From tests/helpers.cjs:
```javascript
function runGsdToolsFull(args, cwd, envOverrides) → { success, output, stderr, error }
function createLegacyProject() → tmpDir  // config.json without concurrent:true
function createConcurrentProject(version) → tmpDir  // config.json with concurrent:true + workspace
function cleanup(tmpDir) → void
```

<!-- CLI commands to test (all return JSON with --raw) -->

init plan-phase <phaseNum> [--raw]:
  Returns: { phase_dir, phase_number, has_research, has_plans, planning_root, layout_style, milestone_scope, ... }

init execute-phase <phaseNum> [--raw]:
  Returns: { phase_dir, state_path, roadmap_path, layout_style, milestone_scope, planning_root, ... }

phase complete <phaseNum> [--raw]:
  Returns: { phase_completed, roadmap_updated, ... }
  Side effect: marks phase as [x] in ROADMAP.md

<!-- Routing pattern for milestone-scoped commands -->
CLI format: node gsd-tools.cjs --milestone v2.0 <command> <subcommand> <args> --raw
The --milestone flag MUST precede the command name.

<!-- E2E test approach from research -->
- Legacy mode: createLegacyProject() with STATE.md + ROADMAP.md, phases dir with phase subdirectory
- Milestone mode: createConcurrentProject('v2.0') with full workspace scaffold
- Test: init plan-phase, init execute-phase, phase complete in each mode
- Phase complete requires: ROADMAP.md with phase entry, phases dir with PLAN.md + SUMMARY.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E test file with legacy mode lifecycle tests</name>
  <files>tests/e2e.test.cjs</files>
  <action>
Create `tests/e2e.test.cjs` with the following structure:

**File header:**
```javascript
/**
 * GSD Tools Tests - End-to-end lifecycle (TEST-06)
 * Tests plan→execute→verify flow in both legacy and milestone-scoped modes.
 */
const { describe, test, before, after } = require('node:test');
const assert = require('node:assert');
const fs = require('fs');
const path = require('path');
const { runGsdToolsFull, createLegacyProject, createConcurrentProject, cleanup } = require('./helpers.cjs');
```

**Describe block 1: `'E2E: legacy mode plan→execute→verify'`**

Use `before()` to scaffold a legacy project with:
- `createLegacyProject()` to get tmpDir with config.json (no concurrent)
- Write ROADMAP.md at `.planning/ROADMAP.md` with this content:
  ```
  # Roadmap\n\n- [ ] Phase 1: E2E Test Phase\n\n### Phase 1: E2E Test Phase\n**Goal:** E2E test\n**Requirements:** [E2E-01]\n**Plans:** 1 plans\n\n| Phase | Status |\n|-------|--------|\n| 1. E2E Test Phase | Planned |\n
  ```
- Write STATE.md at `.planning/STATE.md` with `# State\n`
- Create phase directory: `.planning/phases/01-e2e-test-phase/`
- Write a minimal PLAN.md at `01-e2e-test-phase/01-01-PLAN.md`: `---\nphase: 01-e2e-test-phase\nplan: 01\n---\n# Plan\n`
- Write a minimal SUMMARY.md at `01-e2e-test-phase/01-01-SUMMARY.md`: `# Summary\n`

Use `after()` to `cleanup(tmpDir)`.

**Tests in legacy describe block:**

1. `'init plan-phase returns legacy layout_style and paths'`
   - Run `runGsdToolsFull(['init', 'plan-phase', '1', '--raw'], tmpDir)`
   - Assert `result.success` is true
   - Parse JSON output
   - Assert `out.layout_style === 'legacy'`
   - Assert `out.planning_root` ends with `/.planning` (use `endsWith`)
   - Assert `out.milestone_scope === null`

2. `'init execute-phase returns legacy layout_style and state_path'`
   - Run `runGsdToolsFull(['init', 'execute-phase', '1', '--raw'], tmpDir)`
   - Assert `result.success` is true
   - Parse JSON output
   - Assert `out.layout_style === 'legacy'`
   - Assert `out.state_path === '.planning/STATE.md'` (relative path)

3. `'phase complete updates legacy ROADMAP.md'`
   - Run `runGsdToolsFull(['phase', 'complete', '1', '--raw'], tmpDir)`
   - Assert `result.success` is true
   - Read `.planning/ROADMAP.md` from tmpDir
   - Assert content includes `[x]` (phase marked complete)

  <quality_scan>
    <code_to_reuse>
      - Known: `tests/routing.test.cjs` — Group 6 has the closest pattern for phase complete testing with milestone-scoped setup
      - Known: `tests/helpers.cjs` — `createLegacyProject()`, `runGsdToolsFull()`, `cleanup()`
      - Grep: `grep -n "init.*plan-phase\|init.*execute-phase\|phase.*complete" tests/routing.test.cjs | head -10`
    </code_to_reuse>
    <docs_to_consult>
      N/A — no external library dependencies. Uses Node.js built-in `node:test` and `node:assert`.
    </docs_to_consult>
    <tests_to_write>
      - File: `tests/e2e.test.cjs` (new)
      - What to test: init plan-phase in legacy mode, init execute-phase in legacy mode, phase complete in legacy mode
    </tests_to_write>
  </quality_scan>
  </action>
  <verify>
    <automated>cd /Users/tmac/Projects/gsdup && node --test tests/e2e.test.cjs 2>&1 | tail -10</automated>
  </verify>
  <done>tests/e2e.test.cjs exists with legacy mode describe block. 3 tests: init plan-phase (legacy), init execute-phase (legacy), phase complete (legacy). All pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add milestone-scoped mode lifecycle tests to E2E file</name>
  <files>tests/e2e.test.cjs</files>
  <action>
Add a second describe block to `tests/e2e.test.cjs`:

**Describe block 2: `'E2E: milestone-scoped mode plan→execute→verify'`**

Use `before()` to scaffold a milestone-scoped project with:
- `createConcurrentProject('v2.0')` to get tmpDir with config.json (`concurrent: true`) and workspace
- Write ROADMAP.md at `.planning/milestones/v2.0/ROADMAP.md` with:
  ```
  # Roadmap\n\n- [ ] Phase 1: E2E Test Phase\n\n### Phase 1: E2E Test Phase\n**Goal:** E2E test\n**Requirements:** [E2E-01]\n**Plans:** 1 plans\n\n| Phase | Status |\n|-------|--------|\n| 1. E2E Test Phase | Planned |\n
  ```
- Write STATE.md at `.planning/milestones/v2.0/STATE.md` with structured content matching what init commands expect:
  ```
  # State\n\n**Current Phase:** 1\n**Current Plan:** 01\n**Status:** Active\n**Current Phase Name:** E2E Test Phase\n**Last Activity:** 2026-01-01\n**Last Activity Description:** Testing\n
  ```
- Create phase directory in BOTH locations:
  - `.planning/milestones/v2.0/phases/01-e2e-test-phase/` (for milestone workspace)
  - `.planning/phases/01-e2e-test-phase/` (phase-op uses root phases dir for search, then resolves to milestone workspace)
- Write PLAN.md at `milestones/v2.0/phases/01-e2e-test-phase/01-01-PLAN.md`: `---\nphase: 01-e2e-test-phase\nplan: 01\n---\n# Plan\n`
- Write SUMMARY.md at `milestones/v2.0/phases/01-e2e-test-phase/01-01-SUMMARY.md`: `# Summary\n`

Use `after()` to `cleanup(tmpDir)`.

**Tests in milestone-scoped describe block:**

1. `'init plan-phase with --milestone returns milestone-scoped layout_style'`
   - Run `runGsdToolsFull(['--milestone', 'v2.0', 'init', 'plan-phase', '1', '--raw'], tmpDir)`
   - Assert `result.success` is true
   - Parse JSON output
   - Assert `out.layout_style === 'milestone-scoped'`
   - Assert `out.planning_root` includes `'milestones/v2.0'`
   - Assert `out.milestone_scope === 'v2.0'`

2. `'init execute-phase with --milestone returns milestone workspace paths'`
   - Run `runGsdToolsFull(['--milestone', 'v2.0', 'init', 'execute-phase', '1', '--raw'], tmpDir)`
   - Assert `result.success` is true
   - Parse JSON output
   - Assert `out.layout_style === 'milestone-scoped'`
   - Assert `out.state_path` includes `'milestones/v2.0'`

3. `'phase complete with --milestone updates milestone workspace ROADMAP.md'`
   - Run `runGsdToolsFull(['--milestone', 'v2.0', 'phase', 'complete', '1', '--raw'], tmpDir)`
   - Assert `result.success` is true
   - Read ROADMAP.md from `.planning/milestones/v2.0/ROADMAP.md`
   - Assert content includes `[x]`

4. `'phase complete with --milestone does not modify root ROADMAP.md'`
   - Check if `.planning/ROADMAP.md` exists in tmpDir (it should not exist for a concurrent project, but if it does)
   - If it exists, read content and assert it does NOT contain `[x]`

  <quality_scan>
    <code_to_reuse>
      - Known: `tests/routing.test.cjs` — Groups 1-5 and Group 6 established all milestone-scoped CLI invocation patterns
      - Known: `tests/e2e.test.cjs` — Task 1 just created the file structure with legacy mode tests; follow same pattern
      - Grep: `grep -n "milestone.*init\|phase.*complete.*milestone" tests/routing.test.cjs | head -10`
    </code_to_reuse>
    <docs_to_consult>
      N/A — no external library dependencies. Uses Node.js built-in `node:test` and `node:assert`.
    </docs_to_consult>
    <tests_to_write>
      - File: `tests/e2e.test.cjs` (same file, second describe block)
      - What to test: init plan-phase (milestone), init execute-phase (milestone), phase complete (milestone), isolation (root ROADMAP unmodified)
    </tests_to_write>
  </quality_scan>
  </action>
  <verify>
    <automated>cd /Users/tmac/Projects/gsdup && node --test tests/e2e.test.cjs 2>&1 | tail -10</automated>
  </verify>
  <done>tests/e2e.test.cjs has two describe blocks: legacy mode (3 tests) and milestone-scoped mode (4 tests). All 7 tests pass. Both layout modes exercise the full init-plan-phase → init-execute-phase → phase-complete lifecycle via real CLI invocations.</done>
</task>

</tasks>

<verification>
Run the E2E test file in isolation:

```bash
cd /Users/tmac/Projects/gsdup && node --test tests/e2e.test.cjs 2>&1
```

Expected: 7 tests pass (3 legacy + 4 milestone-scoped), 0 fail.

Run the full test suite to confirm no regressions:

```bash
cd /Users/tmac/Projects/gsdup && node --test tests/*.test.cjs 2>&1 | grep "^# "
```

Expected: `# pass` count increases by 7, `# fail 3` (unchanged pre-existing failures).
</verification>

<success_criteria>
- tests/e2e.test.cjs exists with 7 tests (3 legacy + 4 milestone-scoped)
- All 7 tests pass
- Legacy mode: init plan-phase, init execute-phase, and phase complete all return correct layout_style and paths
- Milestone mode: all commands with --milestone return milestone-scoped paths and modify only milestone workspace files
- No existing tests broken
- TEST-06 requirement fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/13-test-coverage/13-02-SUMMARY.md`
</output>
