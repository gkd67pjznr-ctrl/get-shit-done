---
phase: 06-commands-and-ux
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - commands/gsd/set-quality.md
  - get-shit-done/workflows/set-quality.md
  - get-shit-done/workflows/help.md
  - get-shit-done/workflows/progress.md
autonomous: true
requirements:
  - QCFG-01
  - QCFG-04
  - QOBS-02
  - INFR-02

must_haves:
  truths:
    - "User can type /gsd:set-quality strict and see quality level change confirmed"
    - "User can type /gsd:set-quality --global standard and see global defaults update confirmed"
    - "/gsd:progress output shows the current quality level alongside the progress bar"
    - "/gsd:help output shows a reapply-patches reminder when patches exist"
  artifacts:
    - path: "commands/gsd/set-quality.md"
      provides: "Slash command entry point for /gsd:set-quality"
      contains: "gsd:set-quality"
    - path: "get-shit-done/workflows/set-quality.md"
      provides: "Workflow logic for set-quality command"
      contains: "set-quality"
    - path: "get-shit-done/workflows/help.md"
      provides: "Help reference with patches reminder logic"
      contains: "check-patches"
    - path: "get-shit-done/workflows/progress.md"
      provides: "Progress workflow with quality level display"
      contains: "quality"
  key_links:
    - from: "commands/gsd/set-quality.md"
      to: "get-shit-done/workflows/set-quality.md"
      via: "execution_context reference"
      pattern: "set-quality\\.md"
    - from: "get-shit-done/workflows/set-quality.md"
      to: "get-shit-done/bin/gsd-tools.cjs"
      via: "bash invocation of set-quality CLI command"
      pattern: "gsd-tools\\.cjs set-quality"
    - from: "get-shit-done/workflows/help.md"
      to: "get-shit-done/bin/gsd-tools.cjs"
      via: "bash invocation of check-patches CLI command"
      pattern: "gsd-tools\\.cjs check-patches"
---

<objective>
Create the user-facing UX layer: /gsd:set-quality slash command with workflow, update /gsd:progress to display quality level, and update /gsd:help to show reapply-patches reminder.

Purpose: Wire Plan 1's tested backend commands into the user-facing GSD command system so users can actually invoke them.
Output: 2 new files (set-quality command + workflow), 2 updated files (help + progress workflows).
</objective>

<execution_context>
@/Users/tmac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tmac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-commands-and-ux/06-01-SUMMARY.md

<interfaces>
<!-- Key patterns from Plan 1's CLI commands that these UX files will invoke -->

From gsd-tools.cjs CLI (created in Plan 1):
```bash
# Set quality level for current project (QCFG-01)
node ~/.claude/get-shit-done/bin/gsd-tools.cjs set-quality <fast|standard|strict>
# Output: {"updated":true,"level":"strict","scope":"project"}

# Set quality level globally (QCFG-04)
node ~/.claude/get-shit-done/bin/gsd-tools.cjs set-quality --global <fast|standard|strict>
# Output: {"updated":true,"level":"standard","scope":"global"}

# Check for pending patches (INFR-02)
node ~/.claude/get-shit-done/bin/gsd-tools.cjs check-patches
# Output: {"has_patches":false} or {"has_patches":true,"file_count":2,"from_version":"1.0.0"}

# Progress now includes quality_level in JSON output (QOBS-02)
node ~/.claude/get-shit-done/bin/gsd-tools.cjs progress json
# Output includes: "quality_level": "fast"
```

From existing command pattern (set-profile.md):
```markdown
<!-- commands/gsd/set-profile.md structure — reuse this pattern for set-quality -->
---
name: gsd:set-profile
description: Switch model profile...
argument-hint: <profile>
allowed-tools: [Read, Write, Bash]
---
<objective>...</objective>
<execution_context>@~/.claude/get-shit-done/workflows/set-profile.md</execution_context>
<process>Follow the workflow...</process>
```

From existing workflow pattern (set-profile.md):
```markdown
<!-- workflows/set-profile.md structure — reuse this pattern for set-quality -->
<purpose>...</purpose>
<process>
  <step name="validate">Validate argument</step>
  <step name="ensure_and_load_config">Ensure config exists</step>
  <step name="update_config">Update config</step>
  <step name="confirm">Display confirmation</step>
</process>
<success_criteria>...</success_criteria>
```

From existing progress workflow (progress.md):
```markdown
<!-- The report step currently shows: -->
**Progress:** {PROGRESS_BAR}
**Profile:** [quality/balanced/budget]
<!-- QOBS-02: Add **Quality:** line after Profile -->
```

From existing help workflow (help.md):
```markdown
<!-- Currently just a <reference> block with static content -->
<!-- INFR-02: Add a dynamic step before the reference that checks for patches -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /gsd:set-quality slash command and workflow</name>
  <files>
    commands/gsd/set-quality.md
    get-shit-done/workflows/set-quality.md
  </files>
  <action>
**A. Create `commands/gsd/set-quality.md`:**

Follow the exact structure of `commands/gsd/set-profile.md` as template. Key fields:

```yaml
---
name: gsd:set-quality
description: Set quality enforcement level (fast/standard/strict) for project or globally
argument-hint: <level> [--global]
allowed-tools:
  - Read
  - Write
  - Bash
---
```

The `<objective>` should explain: Sets quality.level for the current project (default) or globally via --global. The `<execution_context>` should reference `@~/.claude/get-shit-done/workflows/set-quality.md`. The `<process>` should say to follow the workflow.

**B. Create `get-shit-done/workflows/set-quality.md`:**

Follow the `set-profile.md` workflow pattern but adapted for quality levels:

```xml
<purpose>
Set the quality enforcement level for GSD commands. Controls how thoroughly the quality sentinel checks code during execution.
</purpose>

<process>

<step name="validate">
Validate argument:
- If $ARGUMENTS is empty or not in ["fast", "standard", "strict"]:
  Error: Invalid quality level. Valid levels: fast, standard, strict
  - fast: Skip all quality gates (fastest execution)
  - standard: Run quality gates, warn on issues (default for new projects)
  - strict: Run quality gates, block on issues
  EXIT
</step>

<step name="detect_scope">
Check for --global flag in $ARGUMENTS:
- If --global present: scope = "global"
- Otherwise: scope = "project"
</step>

<step name="execute">
Run the CLI command:

```bash
# For project scope:
node ~/.claude/get-shit-done/bin/gsd-tools.cjs set-quality $LEVEL

# For global scope:
node ~/.claude/get-shit-done/bin/gsd-tools.cjs set-quality --global $LEVEL
```
</step>

<step name="confirm">
Display confirmation:

For project scope:
```
Quality level set to: $LEVEL

| Level | Behavior |
|-------|----------|
| fast | Skip all quality gates |
| standard | Run gates, warn on issues |
| strict | Run gates, block on issues |

Current: $LEVEL
Scope: project (.planning/config.json)

Subsequent commands will enforce the new level.
```

For global scope:
```
Global quality default set to: $LEVEL

New projects initialized after this will inherit quality.level=$LEVEL.
Existing projects are not affected (update them with /gsd:set-quality $LEVEL).

Scope: global (~/.gsd/defaults.json)
```
</step>

</process>

<success_criteria>
- [ ] Level argument validated
- [ ] Scope detected (project vs global)
- [ ] Config updated via CLI command
- [ ] Confirmation displayed with level table
</success_criteria>
```

Also add `/gsd:set-quality` to the help reference. In `get-shit-done/workflows/help.md`, add a new entry under the `### Configuration` section (after `/gsd:set-profile`):

```markdown
**`/gsd:set-quality <level>`**
Set quality enforcement level (fast/standard/strict).

- `fast` — Skip all quality gates (fastest execution)
- `standard` — Run quality gates, warn on issues
- `strict` — Run quality gates, block on issues
- Add `--global` to set default for new projects

Usage: `/gsd:set-quality strict`
Usage: `/gsd:set-quality --global standard`
```

  <quality_scan>
    <code_to_reuse>
      - Known: `commands/gsd/set-profile.md` — exact frontmatter structure and process pattern for the slash command file
      - Known: `get-shit-done/workflows/set-profile.md` — step structure (validate, ensure_and_load_config, update, confirm) for the workflow file
      - Grep: `grep -rn "gsd:set-profile\|argument-hint" commands/gsd/set-profile.md | head -10`
    </code_to_reuse>
    <docs_to_consult>
      N/A — no external library dependencies, following established GSD workflow patterns
    </docs_to_consult>
    <tests_to_write>
      N/A — no new exported logic (markdown workflow files)
    </tests_to_write>
  </quality_scan>
  </action>
  <verify>
    <automated>test -f /Users/tmac/Projects/gsdup/commands/gsd/set-quality.md && test -f /Users/tmac/Projects/gsdup/get-shit-done/workflows/set-quality.md && grep -q "gsd:set-quality" /Users/tmac/Projects/gsdup/get-shit-done/workflows/help.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    /gsd:set-quality command file and workflow created. Help reference updated with set-quality entry.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update help workflow for patches reminder and progress workflow for quality level</name>
  <files>
    get-shit-done/workflows/help.md
    get-shit-done/workflows/progress.md
  </files>
  <action>
**A. Update `get-shit-done/workflows/help.md` for INFR-02 (patches reminder):**

The help workflow currently has a `<purpose>` and a `<reference>` block with static content. Add a dynamic patches check BEFORE the reference output.

Change the workflow structure so it:
1. First runs `check-patches` to see if patches exist
2. If patches exist, prepends a reminder banner before the reference content

Add a new `<process>` section that wraps the existing behavior:

```xml
<process>

<step name="check_patches">
Check for pending local patches:

```bash
PATCHES=$(node ~/.claude/get-shit-done/bin/gsd-tools.cjs check-patches)
```

Parse the JSON output. If `has_patches` is true, display a reminder banner BEFORE the reference:

```
---
**Patches need reapplying.** You have {file_count} local modification(s) from v{from_version} that need to be merged into the current GSD version.

Run: `/gsd:reapply-patches`
---

```

If `has_patches` is false, skip the banner entirely (display nothing extra).
</step>

<step name="show_reference">
Output the complete GSD command reference from the <reference> section below.
Display the reference content directly — no additions or modifications beyond the patches banner above.
</step>

</process>
```

The existing `<reference>` block stays unchanged. The `<purpose>` tag content should be updated to mention the patches check.

**B. Update `get-shit-done/workflows/progress.md` for QOBS-02 (quality level display):**

In the `report` step, the progress output template currently shows:

```
**Progress:** {PROGRESS_BAR}
**Profile:** [quality/balanced/budget]
```

Add a quality level line. After the Profile line, add:

```
**Quality:** [fast/standard/strict]
```

To get the quality level, read it from the progress JSON output which now includes `quality_level` (from Plan 1):

In the `load` step or `report` step, extract quality level:
```bash
QUALITY_LEVEL=$(node ~/.claude/get-shit-done/bin/gsd-tools.cjs config-get quality.level 2>/dev/null || echo 'fast')
```

Add `**Quality:** $QUALITY_LEVEL` to the report template between `**Profile:**` and `## Recent Work`.

  <quality_scan>
    <code_to_reuse>
      - Known: `get-shit-done/workflows/help.md` — current static reference structure to wrap with patches check
      - Known: `get-shit-done/workflows/progress.md` — report step template at approximately line 87-128 for quality level insertion
      - Grep: `grep -n "Profile\|Progress\|report" get-shit-done/workflows/progress.md | head -10`
    </code_to_reuse>
    <docs_to_consult>
      N/A — no external library dependencies
    </docs_to_consult>
    <tests_to_write>
      N/A — no new exported logic (markdown workflow files with dynamic bash invocations)
    </tests_to_write>
  </quality_scan>
  </action>
  <verify>
    <automated>grep -q "check-patches" /Users/tmac/Projects/gsdup/get-shit-done/workflows/help.md && grep -q "quality" /Users/tmac/Projects/gsdup/get-shit-done/workflows/progress.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    help.md shows reapply-patches reminder when patches exist. progress.md displays current quality level in the report.
  </done>
</task>

</tasks>

<verification>
- `commands/gsd/set-quality.md` exists with correct frontmatter and execution_context reference
- `get-shit-done/workflows/set-quality.md` exists with validate, detect_scope, execute, and confirm steps
- `get-shit-done/workflows/help.md` contains check-patches invocation and conditional banner logic
- `get-shit-done/workflows/help.md` reference section includes /gsd:set-quality entry
- `get-shit-done/workflows/progress.md` report step includes quality level display
</verification>

<success_criteria>
- /gsd:set-quality command is discoverable (listed in help, has slash command)
- /gsd:set-quality workflow validates input, calls CLI backend, shows confirmation
- /gsd:help shows patches reminder when patches exist, silent otherwise
- /gsd:progress report includes quality level alongside progress bar
</success_criteria>

<output>
After completion, create `.planning/phases/06-commands-and-ux/06-02-SUMMARY.md`
</output>
