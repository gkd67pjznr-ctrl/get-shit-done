---
phase: 05-housekeeping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-executor.md
  - agents/gsd-verifier.md
autonomous: true
requirements:
  - MAINT-01
  - MAINT-02

must_haves:
  truths:
    - "The repo copies of gsd-executor.md and gsd-verifier.md contain the Debt Auto-Log sections that currently exist only in the installed copies"
    - "fix-debt.md has exactly one copy in the repo; no second copy exists"
    - "After running the installer, the installed copies match the repo copies (modulo path expansion)"
  artifacts:
    - path: "agents/gsd-executor.md"
      provides: "Executor agent with Debt Auto-Log Protocol section"
      contains: "Debt Auto-Log Protocol"
    - path: "agents/gsd-verifier.md"
      provides: "Verifier agent with Step 7c Debt Auto-Log section"
      contains: "Step 7c: Debt Auto-Log"
    - path: "commands/gsd/fix-debt.md"
      provides: "Single authoritative fix-debt command"
  key_links:
    - from: "agents/gsd-executor.md"
      to: "~/.claude/get-shit-done/bin/gsd-tools.cjs debt log"
      via: "Debt Auto-Log Protocol bash snippets"
      pattern: "gsd-tools\\.cjs debt log"
    - from: "agents/gsd-verifier.md"
      to: "~/.claude/get-shit-done/bin/gsd-tools.cjs debt log"
      via: "Step 7c bash snippets"
      pattern: "gsd-tools\\.cjs debt log"
---

<objective>
Sync the repo's agent files with the installed copies that have diverged, and verify fix-debt.md has a single authoritative source.

Purpose: The installed copies of gsd-executor.md and gsd-verifier.md at ~/.claude/agents/ contain Debt Auto-Log sections (added during v3.0 development as local patches) that were never merged back to the repo source files. This creates a divergence where the repo files are incomplete. Additionally, MAINT-02 requires confirming fix-debt.md has no dual-copy issue.

Output: Updated agent files in `agents/` directory with Debt Auto-Log sections merged from installed copies, using portable `~/.claude/` paths.
</objective>

<execution_context>
@/Users/tmac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tmac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v3.1/ROADMAP.md
@.planning/milestones/v3.1/STATE.md
@.planning/milestones/v3.1/REQUIREMENTS.md

<interfaces>
<!-- The installed agent files at ~/.claude/agents/ are the "source of truth" for the
     content that needs merging. The repo files at agents/ are the authoritative SOURCE
     that the installer copies from. The installer (bin/install.js) performs path
     expansion: ~/.claude/ → /Users/<user>/.claude/ at install time.

     Key divergence points (installed has, repo lacks):

     gsd-executor.md (installed: 794 lines, repo: 728 lines):
     - After line 262 (end of quality_sentinel table), installed has a "## Debt Auto-Log Protocol"
       section with Trigger A (FIX ATTEMPT LIMIT) and Trigger B (Blocked Gate Outcome)
     - At line ~401, installed has an extra bullet in deviation_rules FIX ATTEMPT LIMIT:
       "Debt auto-log: If quality level is not `fast`, log each deferred issue..."

     gsd-verifier.md (installed: 847 lines, repo: 758 lines):
     - After line 496 (end of Step 7b section), installed has a "## Step 7c: Debt Auto-Log"
       section with severity mapping and implementation bash snippets
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge Debt Auto-Log sections into repo agent files</name>
  <files>agents/gsd-executor.md, agents/gsd-verifier.md</files>
  <action>
Merge the Debt Auto-Log content from the installed copies back into the repo source files. Use portable `~/.claude/` paths (NOT absolute `/Users/tmac/.claude/`).

**gsd-executor.md** — Two changes needed:

1. After the quality_sentinel table (line ~262, after the `| Post-task diff review | Skip | Run | Run |` row), insert the "Debt Auto-Log Protocol" section. Extract it from the installed copy at `~/.claude/agents/gsd-executor.md` lines 263-327. Ensure all paths use `~/.claude/` (the installed copy has `/Users/tmac/.claude/` which must be converted). The section includes:
   - `## Debt Auto-Log Protocol` header with purpose, entry guard, severity gating
   - `### Trigger A: FIX ATTEMPT LIMIT Reached` with bash snippet
   - `### Trigger B: Blocked Gate Outcome (strict mode)` with bash snippet
   - Important note about trigger B only firing for blocked outcomes

2. In the `<deviation_rules>` section, under the FIX ATTEMPT LIMIT rule (after the "Track as" line), add the bullet:
   `- **Debt auto-log:** If quality level is not `fast`, log each deferred issue to DEBT.md via Debt Auto-Log Protocol (Trigger A) in `<quality_sentinel>``

**gsd-verifier.md** — One change needed:

1. After Step 7b (line ~496), insert the "## Step 7c: Debt Auto-Log" section. Extract it from the installed copy at `~/.claude/agents/gsd-verifier.md` lines 497-585. All paths already use `~/.claude/` in the installed verifier (they were not expanded for some reason). The section includes:
   - Purpose, entry guard, scope note
   - Severity gating (WIRE-03) and severity mapping table
   - Provenance (WIRE-04) with fixed `"phase-verification"` source_plan
   - Implementation bash snippet iterating over FINDINGS arrays
   - Important distinctions notes

**Verification approach:** After editing, run a normalized diff between the updated repo files and the installed copies. The ONLY differences should be path expansion (`~/.claude/` in repo vs `/Users/tmac/.claude/` in installed).

  <quality_scan>
    <code_to_reuse>
      - Known: `~/.claude/agents/gsd-executor.md` lines 263-327 — Debt Auto-Log Protocol section; copy content with path normalization
      - Known: `~/.claude/agents/gsd-executor.md` line ~401 — deviation_rules debt auto-log bullet
      - Known: `~/.claude/agents/gsd-verifier.md` lines 497-585 — Step 7c Debt Auto-Log section
      - Grep: `grep -n "Debt Auto-Log\|Step 7c" agents/*.md` to confirm sections are inserted
    </code_to_reuse>
    <docs_to_consult>
      N/A — no external library dependencies
    </docs_to_consult>
    <tests_to_write>
      N/A — no new exported logic (markdown agent files only)
    </tests_to_write>
  </quality_scan>
  </action>
  <verify>
    <automated>diff <(sed 's|/Users/tmac/.claude/|~/.claude/|g' ~/.claude/agents/gsd-executor.md) agents/gsd-executor.md && diff <(sed 's|/Users/tmac/.claude/|~/.claude/|g' ~/.claude/agents/gsd-verifier.md) agents/gsd-verifier.md && echo "PASS: repo matches installed (modulo path expansion)"</automated>
  </verify>
  <done>Both agent files in the repo contain the full Debt Auto-Log sections. A normalized diff (replacing absolute paths with ~/.claude/) shows zero differences between repo and installed copies.</done>
</task>

<task type="auto">
  <name>Task 2: Verify fix-debt.md single-copy status and document install mechanism</name>
  <files>agents/gsd-executor.md, agents/gsd-verifier.md</files>
  <action>
Verify MAINT-02 is satisfied — fix-debt.md has a single authoritative copy.

1. Run `find . -name "fix-debt.md" -not -path "*/node_modules/*"` to confirm exactly one copy exists in the repo at `commands/gsd/fix-debt.md`.

2. Run `diff <(sed 's|/Users/tmac/.claude/|~/.claude/|g' ~/.claude/commands/gsd/fix-debt.md) commands/gsd/fix-debt.md` to confirm the installed copy matches the repo copy (modulo path expansion).

3. If both checks pass, MAINT-02 is satisfied with no file changes needed. The install mechanism in `bin/install.js` handles path expansion from `~/.claude/` to absolute paths at install time, which is the expected and correct behavior.

No file modifications needed for this task — it is a verification-only task confirming the current state meets MAINT-02.

  <quality_scan>
    <code_to_reuse>
      - Grep: `find . -name "fix-debt.md" -not -path "*/node_modules/*"` to count copies
    </code_to_reuse>
    <docs_to_consult>
      N/A — no external library dependencies
    </docs_to_consult>
    <tests_to_write>
      N/A — no new exported logic
    </tests_to_write>
  </quality_scan>
  </action>
  <verify>
    <automated>test $(find . -name "fix-debt.md" -not -path "*/node_modules/*" | wc -l) -eq 1 && diff <(sed 's|/Users/tmac/.claude/|~/.claude/|g' ~/.claude/commands/gsd/fix-debt.md) commands/gsd/fix-debt.md > /dev/null && echo "PASS: fix-debt.md has single authoritative copy"</automated>
  </verify>
  <done>Exactly one copy of fix-debt.md exists in the repo at commands/gsd/fix-debt.md. The installed copy at ~/.claude/commands/gsd/fix-debt.md is an installer-generated derivative with path expansion only.</done>
</task>

</tasks>

<verification>
1. `git ls-files agents/gsd-executor.md agents/gsd-verifier.md` confirms both are tracked
2. `grep -c "Debt Auto-Log" agents/gsd-executor.md` returns >= 1
3. `grep -c "Step 7c: Debt Auto-Log" agents/gsd-verifier.md` returns 1
4. Normalized diffs between repo and installed copies show zero differences
5. `find . -name "fix-debt.md" -not -path "*/node_modules/*" | wc -l` returns 1
</verification>

<success_criteria>
- agents/gsd-executor.md contains the Debt Auto-Log Protocol section and deviation_rules bullet
- agents/gsd-verifier.md contains the Step 7c: Debt Auto-Log section
- Both files use portable ~/.claude/ paths (not absolute paths)
- Normalized diff between repo and installed copies shows zero differences for all three files
- fix-debt.md has exactly one copy in the repo
</success_criteria>

<output>
After completion, create `.planning/milestones/v3.1/phases/05-housekeeping/05-01-SUMMARY.md`
</output>
