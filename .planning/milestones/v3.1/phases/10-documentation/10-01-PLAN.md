---
phase: 10-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "README.md exists at repo root"
    - "README covers what the fork adds (quality enforcement, concurrent milestones, tech debt system)"
    - "A reader unfamiliar with the project can understand the fork's purpose and key features from README alone"
    - "README reflects the simplified milestone-only architecture with no references to legacy layout or deleted features"
    - "Quick Stats table shows accurate numbers for tests, suites, and source modules"
  artifacts:
    - path: "README.md"
      provides: "Fork identity and feature summary"
      contains: "GSD Enhanced Fork"
  key_links:
    - from: "README.md"
      to: "UPGRADES.md"
      via: "markdown link"
      pattern: "\\[UPGRADES\\.md\\]"
    - from: "README.md"
      to: "FORK-GUIDE.md"
      via: "markdown link"
      pattern: "\\[FORK-GUIDE\\.md\\]"
---

<objective>
Update the existing README.md fork header section to fix stale stats and remove references to deleted features.

Purpose: The README is the first thing a visitor sees. It must reflect the current state of the codebase after v3.1 cleanup work (migrate.cjs deleted, tests expanded from 298 to 710, suites from 14 to 21, lib modules from 13 to 12).

Output: Updated README.md with accurate fork header (lines 1-37 only; upstream content lines 39+ untouched).
</objective>

<execution_context>
@/Users/tmac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tmac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v3.1/ROADMAP.md
@.planning/milestones/v3.1/REQUIREMENTS.md
@.planning/milestones/v3.1/STATE.md
@.planning/milestones/v3.1/phases/10-documentation/10-RESEARCH.md

@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md fork header with accurate stats and remove deleted feature reference</name>
  <files>README.md</files>
  <action>
Make exactly three edits to the fork header section of README.md (lines 1-37). Do NOT modify anything at or below line 38 (the upstream GSD content starting with the div tag).

**Edit 1 — Line 20 (Tech Debt System row):** Remove ", planning layout migration tool" from the end of the Tech Debt System row. migrate.cjs was deleted in Phase 7 (STRIP-01) and must not appear as a current feature.

Before:
```
| Tech Debt System | v3.0 | DEBT.md with TD-NNN entries, `debt log/list/resolve` CLI commands, executor/verifier auto-logging, `/gsd:fix-debt` skill, planning layout migration tool |
```

After:
```
| Tech Debt System | v3.0 | DEBT.md with TD-NNN entries, `debt log/list/resolve` CLI commands, executor/verifier auto-logging, `/gsd:fix-debt` skill |
```

**Edit 2 — Line 27 (Tests passing row):** Update from "298 across 14 suites" to "710 across 21 suites". The test count is 710 (from STATE.md Phase 9 completion) and there are 21 test suite files in `tests/*.test.cjs`.

Before:
```
| Tests passing | 298 across 14 suites |
```

After:
```
| Tests passing | 710 across 21 suites |
```

**Edit 3 — Line 28 (Source modules row):** Update from "13 lib modules" to "12 lib modules". migrate.cjs was deleted in Phase 7, reducing the count from 13 to 12 modules in `get-shit-done/bin/lib/`.

Before:
```
| Source modules | 13 lib modules |
```

After:
```
| Source modules | 12 lib modules |
```

**Do NOT:**
- Modify any content below line 37 (the `---` separator before upstream content)
- Change the milestones count (4 is correct -- v3.1 is not yet shipped)
- Add any references to legacy layout, detectLayoutStyle, or migration
- Edit UPGRADES.md or FORK-GUIDE.md
  </action>
  <verify>
    <automated>grep -n "planning layout migration tool" README.md && echo "FAIL: migrate reference still present" && exit 1; grep "710 across 21 suites" README.md && grep "12 lib modules" README.md && echo "PASS: all three edits verified" || (echo "FAIL: stats not updated" && exit 1)</automated>
  </verify>
  <done>
- Line 20: "planning layout migration tool" removed from Tech Debt System row
- Line 27: Tests count updated to "710 across 21 suites"
- Line 28: Source modules updated to "12 lib modules"
- Lines 38+ (upstream content) unchanged
- No references to legacy layout, detectLayoutStyle, or migration in the fork header
  </done>
</task>

</tasks>

<verification>
1. `grep -c "planning layout migration tool" README.md` returns 0
2. `grep "710 across 21 suites" README.md` matches
3. `grep "12 lib modules" README.md` matches
4. `grep "4 (v1.0 through v3.0)" README.md` matches (milestones count unchanged)
5. `wc -l < README.md` returns approximately 743 (same as before, minor line count variation acceptable)
6. `head -37 README.md | grep -ic "legacy\|detectLayoutStyle\|migrate"` returns 0
</verification>

<success_criteria>
README.md fork header (lines 1-37) accurately reflects the current state of the codebase:
- No reference to deleted features (migrate.cjs)
- Test count matches Phase 9 completion (710 across 21 suites)
- Source module count matches current lib directory (12 modules)
- Upstream content (lines 38+) untouched
- All four ROADMAP success criteria for Phase 10 are met
</success_criteria>

<output>
After completion, create `.planning/milestones/v3.1/phases/10-documentation/10-01-SUMMARY.md`
</output>
