---
phase: 04-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/bin/lib/milestone.cjs
  - get-shit-done/workflows/execute-plan.md
  - tests/milestone-complete.test.cjs
autonomous: true
requirements:
  - FIX-01
  - FIX-02
must_haves:
  truths:
    - "cmdMilestoneComplete resolves phasesDir from planningRoot(cwd, milestoneScope) instead of hardcoding .planning/phases/"
    - "execute-plan.md passes --milestone flag to roadmap update-plan-progress when milestone_scope is set"
  artifacts:
    - path: "get-shit-done/bin/lib/milestone.cjs"
      provides: "Milestone-scope-aware cmdMilestoneComplete"
      contains: "planningRoot"
    - path: "get-shit-done/workflows/execute-plan.md"
      provides: "Milestone-aware roadmap update step"
      contains: "--milestone"
    - path: "tests/milestone-complete.test.cjs"
      provides: "Tests for milestone complete in milestone-scoped layout"
  key_links:
    - from: "get-shit-done/bin/lib/milestone.cjs"
      to: "get-shit-done/bin/lib/core.cjs"
      via: "planningRoot import"
      pattern: "planningRoot\\(cwd"
---

<objective>
Fix cmdMilestoneComplete phasesDir resolution and execute-plan.md --milestone pass-through.

Purpose: In a milestone-scoped layout, `gsd milestone complete` fails because it reads phases from the wrong directory, and `roadmap update-plan-progress` during plan execution does not receive the `--milestone` flag, causing it to update the wrong ROADMAP.md.

Output: Working milestone complete in milestone-scoped layouts; milestone-aware roadmap progress updates during execution.
</objective>

<execution_context>
@/Users/tmac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tmac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v3.1/ROADMAP.md
@.planning/milestones/v3.1/STATE.md
@get-shit-done/bin/lib/milestone.cjs
@get-shit-done/bin/lib/core.cjs
@get-shit-done/workflows/execute-plan.md

<interfaces>
<!-- Key functions the executor needs -->

From get-shit-done/bin/lib/core.cjs:
```javascript
function planningRoot(cwd, milestoneScope) {
  const base = path.join(cwd, '.planning');
  if (milestoneScope) {
    return path.join(base, 'milestones', milestoneScope);
  }
  return base;
}
// Already imported in milestone.cjs? No — currently only imports: output, error, escapeRegex
```

From get-shit-done/bin/lib/milestone.cjs — cmdMilestoneComplete signature:
```javascript
function cmdMilestoneComplete(cwd, version, options, raw) {
  // BUG at line 87: phasesDir = path.join(cwd, '.planning', 'phases')
  // Should use planningRoot(cwd, milestoneScope) to find phases
}
```

From get-shit-done/bin/gsd-tools.cjs — milestone router (lines 563-578):
```javascript
case 'milestone': {
  // Does NOT pass milestoneScope to cmdMilestoneComplete
  milestone.cmdMilestoneComplete(cwd, args[2], { name: milestoneName, archivePhases }, raw);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix cmdMilestoneComplete phasesDir and wire milestoneScope</name>
  <files>
    get-shit-done/bin/lib/milestone.cjs
    get-shit-done/bin/gsd-tools.cjs
  </files>
  <action>
    **In `get-shit-done/bin/lib/milestone.cjs`:**

    1. Update the import from core.cjs (line 7) to include `planningRoot`:
       Change: `const { output, error, escapeRegex } = require('./core.cjs');`
       To: `const { output, error, escapeRegex, planningRoot } = require('./core.cjs');`

    2. Update the `cmdMilestoneComplete` function signature (line 77) to accept `milestoneScope`:
       Change: `function cmdMilestoneComplete(cwd, version, options, raw)`
       To: `function cmdMilestoneComplete(cwd, version, options, raw, milestoneScope)`

    3. At line 87, replace the hardcoded `phasesDir`:
       Change: `const phasesDir = path.join(cwd, '.planning', 'phases');`
       To: `const phasesDir = path.join(planningRoot(cwd, milestoneScope), 'phases');`

    4. Also fix lines 82-85 — the roadmapPath, reqPath, and statePath should also resolve via planningRoot when milestoneScope is provided:
       Change: `const roadmapPath = path.join(cwd, '.planning', 'ROADMAP.md');`
       To: `const roadmapPath = path.join(planningRoot(cwd, milestoneScope), 'ROADMAP.md');`
       Similarly for reqPath and statePath.

    5. Fix line 86 (milestonesPath) — MILESTONES.md stays at project root, so leave it as `path.join(cwd, '.planning', 'MILESTONES.md')`.

    6. Fix line 86 (archiveDir) — archiveDir stays at project root: `path.join(cwd, '.planning', 'milestones')`. Leave as-is.

    7. The workspaceDir at line 128 references `archiveDir` which should already be `.planning/milestones`, so `path.join(archiveDir, version)` works correctly for both legacy and milestone-scoped.

    **In `get-shit-done/bin/gsd-tools.cjs`:**

    8. Update the milestone complete call in the CLI router (line 578) to pass `milestoneScope`:
       Change: `milestone.cmdMilestoneComplete(cwd, args[2], { name: milestoneName, archivePhases }, raw);`
       To: `milestone.cmdMilestoneComplete(cwd, args[2], { name: milestoneName, archivePhases }, raw, milestoneScope);`

    <quality_scan>
      <code_to_reuse>
        - Known: `get-shit-done/bin/lib/core.cjs` — `planningRoot(cwd, milestoneScope)` resolves root path; import and use directly
        - Known: `get-shit-done/bin/lib/phase.cjs` — `cmdPhaseComplete` already accepts milestoneScope as last param; follow same pattern
        - Grep pattern: `grep -n "planningRoot" get-shit-done/bin/lib/*.cjs | head -20`
      </code_to_reuse>
      <docs_to_consult>
        N/A — no external library dependencies
      </docs_to_consult>
      <tests_to_write>
        - File: `tests/milestone-complete.test.cjs`
        - What to test: cmdMilestoneComplete resolves phases from milestone workspace when milestoneScope is provided (see Task 2)
      </tests_to_write>
    </quality_scan>
  </action>
  <verify>
    <automated>grep -n "planningRoot" get-shit-done/bin/lib/milestone.cjs | head -5 && grep -n "milestoneScope" get-shit-done/bin/gsd-tools.cjs | grep "cmdMilestoneComplete"</automated>
  </verify>
  <done>
    - cmdMilestoneComplete imports planningRoot from core.cjs
    - phasesDir, roadmapPath, reqPath, statePath resolve via planningRoot(cwd, milestoneScope)
    - CLI router passes milestoneScope to cmdMilestoneComplete
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests for milestone complete in milestone-scoped layout</name>
  <files>
    tests/milestone-complete.test.cjs
  </files>
  <action>
    Create `tests/milestone-complete.test.cjs` with tests for the fixed behavior:

    1. Import test utilities: `{ describe, test, before, after }` from `node:test`, `assert` from `node:assert/strict`, `fs`, `path`, and `{ createConcurrentProject, runGsdToolsFull, cleanup }` from `./helpers.cjs`.

    2. **Test group: "FIX-01: milestone complete with --milestone resolves phasesDir correctly"**

       `before()`:
       - Create concurrent project with `createConcurrentProject('v2.0')`
       - Create a milestone workspace with phases: `mkdir -p .planning/milestones/v2.0/phases/01-test-phase`
       - Write a PLAN.md and SUMMARY.md into the milestone phase dir
       - Write ROADMAP.md into the milestone workspace (`.planning/milestones/v2.0/ROADMAP.md`) with a Phase 1 entry
       - Write STATE.md into the milestone workspace
       - Do NOT create phases at `.planning/phases/` — milestone-scoped layout should find them in the workspace

       Tests:
       - `test('milestone complete reads phases from milestone workspace')`: Run `runGsdToolsFull(['--milestone', 'v2.0', 'milestone', 'complete', 'v2.0', '--name', 'Test Milestone'])`. Parse JSON output. Assert `result.phases >= 1` (it found phases in the milestone workspace). Assert `result.plans >= 1`.
       - `test('milestone complete creates MILESTONES.md entry')`: After running complete, verify `.planning/MILESTONES.md` exists and contains 'v2.0'.

    <quality_scan>
      <code_to_reuse>
        - Known: `tests/routing.test.cjs` — `createConcurrentProject('v2.0')` and `runGsdToolsFull` patterns; reuse directly
        - Known: `tests/helpers.cjs` — `createConcurrentProject()`, `cleanup()` helpers
        - Grep pattern: `grep -n "createConcurrentProject\|runGsdToolsFull" tests/*.test.cjs | head -10`
      </code_to_reuse>
      <docs_to_consult>
        N/A — no external library dependencies
      </docs_to_consult>
      <tests_to_write>
        - File: `tests/milestone-complete.test.cjs`
        - What to test: milestone complete resolves phases from milestone workspace when milestoneScope is provided; MILESTONES.md entry created
      </tests_to_write>
    </quality_scan>
  </action>
  <verify>
    <automated>node --test tests/milestone-complete.test.cjs</automated>
  </verify>
  <done>
    - Tests pass confirming cmdMilestoneComplete reads phases from milestone workspace
    - Tests confirm MILESTONES.md is created with correct milestone info
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix execute-plan.md to pass --milestone to roadmap update-plan-progress</name>
  <files>
    get-shit-done/workflows/execute-plan.md
  </files>
  <action>
    **In `get-shit-done/workflows/execute-plan.md`:**

    1. In step `init_context` (line 18), update the init command to pass milestone scope if present:
       Change: `INIT=$(node /Users/tmac/.claude/get-shit-done/bin/gsd-tools.cjs init execute-phase "${PHASE}")`
       To: `INIT=$(node /Users/tmac/.claude/get-shit-done/bin/gsd-tools.cjs init execute-phase "${PHASE}" ${MILESTONE_FLAG})`
       And add a comment: `# MILESTONE_FLAG is set by orchestrator when --milestone is active (e.g., "--milestone v3.1")`

    2. Add `milestone_scope` to the "Extract from init JSON" list at line 21.

    3. In step `update_roadmap` (line 382-384), update to pass milestone flag:
       Change:
       ```
       node /Users/tmac/.claude/get-shit-done/bin/gsd-tools.cjs roadmap update-plan-progress "${PHASE}"
       ```
       To:
       ```
       node /Users/tmac/.claude/get-shit-done/bin/gsd-tools.cjs roadmap update-plan-progress "${PHASE}" ${MILESTONE_FLAG}
       ```
       Where `MILESTONE_FLAG` is derived from the init JSON `milestone_scope` field:
       ```
       # Set MILESTONE_FLAG from init context
       if [ -n "${MILESTONE_SCOPE}" ]; then
         MILESTONE_FLAG="--milestone ${MILESTONE_SCOPE}"
       else
         MILESTONE_FLAG=""
       fi
       ```

    4. Also add the milestone flag to the `update_requirements` step (line 389):
       Change:
       ```
       node /Users/tmac/.claude/get-shit-done/bin/gsd-tools.cjs requirements mark-complete ${REQ_IDS}
       ```
       Note: requirements are project-level not milestone-scoped, so do NOT add --milestone here. Leave as-is.

    5. In step `git_commit_metadata` (line 399-403), the paths are hardcoded to `.planning/`. These are already resolved by the executor from INIT context, so leave as template placeholders.

    6. In step `update_current_position` (lines 335-348), add milestone flag to state commands:
       Add the milestone flag pattern to the `state advance-plan`, `state update-progress`, and `state record-metric` commands since STATE.md is milestone-scoped.
       Change each command to append `${MILESTONE_FLAG}` where state commands accept milestone scope.
       Actually — check: state commands in state.cjs operate on root `.planning/STATE.md`. The milestone STATE.md is at a different path. Since the executor already knows the `state_path` from INIT, the state commands should work on the correct path. The state commands currently don't accept `--milestone`. This is out of scope for FIX-02. Leave state commands as-is.

    The key fix: step `update_roadmap` MUST pass the milestone flag so `roadmap update-plan-progress` reads the correct ROADMAP.md.

    <quality_scan>
      <code_to_reuse>
        - Known: `get-shit-done/workflows/execute-plan.md` — current workflow text; edit in-place
        - Grep pattern: `grep -n "MILESTONE\|milestone" get-shit-done/workflows/execute-plan.md | head -10`
      </code_to_reuse>
      <docs_to_consult>
        N/A — no external library dependencies
      </docs_to_consult>
      <tests_to_write>
        N/A — no new exported logic; this is a workflow markdown file
      </tests_to_write>
    </quality_scan>
  </action>
  <verify>
    <automated>grep -c "MILESTONE" get-shit-done/workflows/execute-plan.md</automated>
  </verify>
  <done>
    - execute-plan.md extracts milestone_scope from init JSON
    - execute-plan.md passes --milestone flag to roadmap update-plan-progress
    - milestone_scope field documented in init context extraction
  </done>
</task>

</tasks>

<verification>
1. `grep -n "planningRoot" get-shit-done/bin/lib/milestone.cjs` shows planningRoot imported and used
2. `grep -n "milestoneScope" get-shit-done/bin/gsd-tools.cjs | grep -i milestone` shows milestoneScope passed through
3. `node --test tests/milestone-complete.test.cjs` passes
4. `grep "MILESTONE" get-shit-done/workflows/execute-plan.md` shows milestone flag in update_roadmap step
</verification>

<success_criteria>
- cmdMilestoneComplete resolves phasesDir from planningRoot instead of hardcoded path
- CLI router passes milestoneScope to cmdMilestoneComplete
- execute-plan.md passes --milestone to roadmap update-plan-progress
- New tests pass for milestone complete in milestone-scoped layout
</success_criteria>

<output>
After completion, create `.planning/milestones/v3.1/phases/04-bug-fixes/04-01-SUMMARY.md`
</output>
