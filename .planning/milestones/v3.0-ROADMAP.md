# Milestone v3.0: Tech Debt System

**Status:** SHIPPED 2026-02-26
**Phases:** 3.1-3.5 (6 phases, including 3.2.1 inserted)
**Total Plans:** 8

## Overview

Build infrastructure for systematic tech debt tracking and resolution, plus a project migration tool for existing `.planning/` folders. Resolved v2.0 integration gaps (INTEGRATION-3, INTEGRATION-4) as foundation work.

## Phases

### Phase 3.1: Integration Fixes

**Goal**: Fix milestone-scoped path resolution bugs so that `cmdInitPlanPhase` and roadmap commands work correctly with milestone-scoped layouts.
**Depends on**: None
**Plans**: 1 plan

Plans:
- [x] 3.1-01: Fix milestone-scoped path resolution in init and roadmap commands with regression tests

### Phase 3.2: Core Debt Module

**Goal**: Create the DEBT.md schema and `debt.cjs` CLI commands for structured tech debt tracking with concurrent-safe writes.
**Depends on**: None
**Plans**: 1 plan

Plans:
- [x] 3.2-01: Create debt.cjs module (all three commands) + test suite + router wiring (TDD)

### Phase 3.2.1: Planning Directory Cleanup and GSD Flow Fixes (INSERTED)

**Goal**: Normalize orphaned/misplaced planning files in `.planning/milestones/`, update stale tracking artifacts, and fix plan-level checkbox updates and milestone workspace finalization.
**Depends on**: Phase 3.2
**Plans**: 2 plans

Plans:
- [x] 3.2.1-01: Normalize milestones directory structure and update stale tracking files
- [x] 3.2.1-02: TDD: Plan-level checkbox flip and milestone workspace finalization code fixes

### Phase 3.3: Migration Tool

**Goal**: Build `migrate.cjs` that inspects and upgrades existing `.planning/` folders with dry-run default, additive-only apply, and undo manifest.
**Depends on**: None
**Plans**: 1 plan

Plans:
- [x] 3.3-01: TDD: migrate.cjs module (inspectLayout + dry-run + apply) with test suite and router wiring

### Phase 3.4: Agent Wiring

**Goal**: Wire executor, verifier, and debugger agents to auto-log discovered debt with quality-level gating.
**Depends on**: Phase 3.2
**Plans**: 2 plans

Plans:
- [x] 3.4-01: Wire executor debt auto-logging at FIX ATTEMPT LIMIT and blocked gate hook points
- [x] 3.4-02: Wire verifier debt auto-logging after Step 7b quality dimension findings

### Phase 3.5: /gsd:fix-debt Skill

**Goal**: Create on-demand orchestrator skill that routes debt entries through diagnosis and fix execution.
**Depends on**: Phase 3.2, Phase 3.4
**Plans**: 1 plan

Plans:
- [x] 3.5-01: Author /gsd:fix-debt orchestrator skill (entry selection, debugger diagnosis, inline fix plan, executor spawn, status resolution)

---

## Milestone Summary

**Decimal Phases:**
- Phase 3.2.1: Planning Directory Cleanup and GSD Flow Fixes (inserted after Phase 3.2 for workspace normalization and flow bug fixes)

**Key Decisions:**
- Shared inspectLayout() function called by both dry-run and apply — prevents output divergence
- Debt logging is additive only — no existing sentinel gate logic modified
- Trigger B (blocked gate) fires in strict mode only — standard mode warned outcomes are informational
- Step 7c in verifier is additive-only — does not affect verification status
- Orphaned exports (medium severity) only logged in strict mode
- Inline mini-plan approach for fix-debt skill — executor spawned directly, bypasses ROADMAP lookup
- Rich-description skip path — yolo mode skips diagnosis for entries with description >50 chars

**Issues Resolved:**
- INTEGRATION-3: cmdInitPlanPhase now uses planningRoot() for milestone-aware paths
- INTEGRATION-4: cmdRoadmapGetPhase and cmdRoadmapAnalyze now respect --milestone flag
- Plan-level checkbox flip in ROADMAP.md (FLOW-01)
- Milestone workspace finalization in cmdMilestoneComplete (FLOW-02)
- Orphaned/misplaced files in .planning/milestones/ normalized to canonical structure

**Issues Deferred:**
- FLOW-02 regression: cmdMilestoneComplete phasesDir still breaks for milestone-scoped layouts (different code path from the one fixed)
- execute-plan.md doesn't pass --milestone flag to roadmap update-plan-progress
- CLI help text incomplete

**Technical Debt Incurred:**
- Agent files (gsd-executor.md, gsd-verifier.md) outside git repo — not version-controlled
- Dual-file maintenance for fix-debt.md (repo copy vs installed copy)
- 2 test artifacts in DEBT.md (TD-001, TD-002)

---

_For current project status, see .planning/ROADMAP.md_
