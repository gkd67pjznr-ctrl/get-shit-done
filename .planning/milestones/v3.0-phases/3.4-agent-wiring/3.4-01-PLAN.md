---
phase: 3.4-agent-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/tmac/.claude/agents/gsd-executor.md
autonomous: true
requirements: [WIRE-01, WIRE-03, WIRE-04]

must_haves:
  truths:
    - "Executor auto-logs debt to DEBT.md when FIX ATTEMPT LIMIT is reached"
    - "Executor auto-logs debt to DEBT.md when a blocked gate outcome occurs in strict mode"
    - "Debt logging is skipped entirely when quality.level is fast"
    - "Debt logging in standard mode only logs critical and high severity entries"
    - "Debt logging in strict mode logs all severity levels"
    - "Every executor-originated debt entry includes source_phase and source_plan provenance"
  artifacts:
    - path: "/Users/tmac/.claude/agents/gsd-executor.md"
      provides: "Debt auto-logging instructions in quality_sentinel and deviation_rules sections"
      contains: "gsd-tools.cjs debt log"
  key_links:
    - from: "/Users/tmac/.claude/agents/gsd-executor.md"
      to: "gsd-tools debt log CLI"
      via: "bash command in instruction text"
      pattern: "debt log.*--logged-by executor"
---

<objective>
Add debt auto-logging instructions to the executor agent at two hook points: (1) FIX ATTEMPT LIMIT in deviation_rules, and (2) blocked gate outcomes in quality_sentinel post-task protocol.

Purpose: When the executor encounters unfixable issues during plan execution, those issues are automatically captured as structured debt entries in DEBT.md for future resolution by /gsd:fix-debt (Phase 3.5).

Output: Modified gsd-executor.md with a `<debt_logging>` subsection and two trigger points.
</objective>

<execution_context>
@/Users/tmac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tmac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v3.0/ROADMAP.md
@.planning/milestones/v3.0/STATE.md
@.planning/milestones/v3.0/phases/3.4-agent-wiring/3.4-RESEARCH.md

<interfaces>
<!-- Existing debt log CLI interface (from gsd-tools.cjs router, verified): -->

```bash
node /Users/tmac/.claude/get-shit-done/bin/gsd-tools.cjs debt log \
  --type <code|test|docs|config|arch> \
  --severity <critical|high|medium|low> \
  --component <name> \
  --description <text> \
  --logged-by <name> \
  --source-phase <phase> \
  --source-plan <plan>
# Returns: { "id": "TD-NNN", "logged": true }
```

<!-- Existing quality level gate pattern (from gsd-executor.md, line 107): -->

```bash
QUALITY_LEVEL=$(node /Users/tmac/.claude/get-shit-done/bin/gsd-tools.cjs config-get quality.level 2>/dev/null || echo "fast")
```

<!-- Existing gate outcome tracking (from gsd-executor.md, lines 127-128): -->

```bash
GATE_OUTCOMES+=("{task_num}|{step_name}|{outcome}|{detail}")
# outcome is one of: passed, warned, skipped, blocked
```

<!-- FIX ATTEMPT LIMIT location (gsd-executor.md, lines 331-336): -->
<!-- Currently: "STOP fixing — document remaining issues in SUMMARY.md under Deferred Issues" -->
<!-- After: also call debt log for each deferred issue, gated on quality level -->

<!-- Blocked gate outcome location (gsd-executor.md, lines 232, 249): -->
<!-- Currently: GATE_OUTCOMES entry recorded with outcome=blocked -->
<!-- After: also call debt log when outcome=blocked, gated on quality level -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add debt auto-logging protocol to executor agent</name>
  <files>/Users/tmac/.claude/agents/gsd-executor.md</files>
  <action>
Edit `/Users/tmac/.claude/agents/gsd-executor.md` to add debt auto-logging at two hook points. All edits are to instruction text (Markdown), not code.

**Change 1: Add `## Debt Auto-Log Protocol` subsection inside `<quality_sentinel>`, after the Gate Behavior Matrix (after line 263, before `</quality_sentinel>`).**

Insert the following subsection:

```markdown
## Debt Auto-Log Protocol

**Purpose:** When unfixable issues are discovered during execution, auto-log them to DEBT.md for future resolution via `/gsd:fix-debt`. This creates a machine-readable record beyond the SUMMARY.md narrative.

**Entry guard:** Uses `QUALITY_LEVEL` already read at sentinel entry. If `fast`, skip all debt logging.

**Severity gating (WIRE-03):**
- `fast`: No debt logging at all
- `standard`: Log only `critical` and `high` severity entries
- `strict`: Log all severity levels (`critical`, `high`, `medium`, `low`)

**Severity assessment for executor-deferred issues:**
- Security vulnerabilities, data corruption risks → `critical`
- Broken tests, missing error handling, correctness failures → `high`
- Code quality issues, missing validation, incomplete coverage → `medium`
- Style issues, naming conventions, minor cleanup → `low`

**Provenance (WIRE-04):** Always pass `--source-phase "${PHASE}"` and `--source-plan "${PHASE}-${PLAN}"` using the variables already in scope from plan frontmatter parsing.

### Trigger A: FIX ATTEMPT LIMIT Reached

After documenting deferred issues in SUMMARY.md (per deviation_rules FIX ATTEMPT LIMIT), also log each deferred issue to DEBT.md:

```bash
if [ "$QUALITY_LEVEL" != "fast" ]; then
  # Assess severity of each deferred issue contextually
  ISSUE_SEVERITY="high"  # Adjust per issue: critical/high/medium/low
  SHOULD_LOG=false
  if [ "$QUALITY_LEVEL" = "strict" ]; then SHOULD_LOG=true; fi
  if [ "$QUALITY_LEVEL" = "standard" ] && { [ "$ISSUE_SEVERITY" = "critical" ] || [ "$ISSUE_SEVERITY" = "high" ]; }; then SHOULD_LOG=true; fi
  if [ "$SHOULD_LOG" = "true" ]; then
    node /Users/tmac/.claude/get-shit-done/bin/gsd-tools.cjs debt log \
      --type code \
      --severity "$ISSUE_SEVERITY" \
      --component "${TASK_FILES}" \
      --description "Executor deferred: ${DEFERRED_ISSUE_DESCRIPTION}" \
      --logged-by executor \
      --source-phase "${PHASE}" \
      --source-plan "${PHASE}-${PLAN}"
  fi
fi
```

### Trigger B: Blocked Gate Outcome (strict mode)

When a gate outcome is `blocked` (strict mode only — standard mode does not produce `blocked` outcomes), log the blocking issue to DEBT.md:

```bash
# After recording GATE_OUTCOMES+=("${TASK_NUM}|${GATE_NAME}|blocked|${DETAIL}")
if [ "$QUALITY_LEVEL" = "strict" ]; then
  node /Users/tmac/.claude/get-shit-done/bin/gsd-tools.cjs debt log \
    --type test \
    --severity high \
    --component "${TASK_FILES}" \
    --description "Strict gate blocked: ${GATE_NAME} — ${DETAIL}" \
    --logged-by executor \
    --source-phase "${PHASE}" \
    --source-plan "${PHASE}-${PLAN}"
fi
```

**Important:** Trigger B fires only for `blocked` outcomes, which only occur in strict mode. Standard mode produces `warned` outcomes which do not trigger debt logging (warnings are informational, captured in SUMMARY.md Quality Gates section only).
```

**Change 2: Add a cross-reference note in `<deviation_rules>`, inside the FIX ATTEMPT LIMIT section (after the line "Continue to the next task (or return checkpoint if blocked)").**

Add this line after the existing FIX ATTEMPT LIMIT bullet points:

```markdown
- **Debt auto-log:** If quality level is not `fast`, log each deferred issue to DEBT.md via Debt Auto-Log Protocol (Trigger A) in `<quality_sentinel>`
```

This ensures the executor sees the debt logging instruction when it hits the FIX ATTEMPT LIMIT, not only when reading the quality sentinel section.

**What NOT to do:**
- Do NOT modify any existing gate outcome tracking or sentinel step logic
- Do NOT add debt logging for `warned` outcomes (standard mode warnings are informational only)
- Do NOT add a new CLI command or .cjs file — use existing `debt log` command
- Do NOT add debt logging to fast mode — the sentinel is already entirely skipped in fast mode

  <quality_scan>
    <code_to_reuse>
      - Known: `/Users/tmac/.claude/agents/gsd-executor.md` lines 107-108 — quality level gate pattern; reuse exact same bash pattern
      - Known: `/Users/tmac/.claude/agents/gsd-executor.md` lines 127-128 — GATE_OUTCOMES tracking pattern
      - Known: `/Users/tmac/Projects/gsdup/get-shit-done/bin/gsd-tools.cjs` lines 49-56 — debt log flag names (verified)
      - Grep pattern: `grep -n "QUALITY_LEVEL\|GATE_OUTCOMES\|FIX ATTEMPT" /Users/tmac/.claude/agents/gsd-executor.md | head -10`
    </code_to_reuse>
    <docs_to_consult>
      N/A — no external library dependencies. All patterns from existing agent instructions and existing CLI.
    </docs_to_consult>
    <tests_to_write>
      N/A — no new exported logic. This is a Markdown instruction file edit. Existing tests should continue passing (no .cjs changes). Verify with: `node --test tests/*.test.cjs 2>&1 | tail -3`
    </tests_to_write>
  </quality_scan>
  </action>
  <verify>
    <automated>grep -c "Debt Auto-Log Protocol" /Users/tmac/.claude/agents/gsd-executor.md && grep -c "debt log" /Users/tmac/.claude/agents/gsd-executor.md && grep -c "logged-by executor" /Users/tmac/.claude/agents/gsd-executor.md && grep -c "source-phase" /Users/tmac/.claude/agents/gsd-executor.md</automated>
  </verify>
  <done>
    - gsd-executor.md contains a "Debt Auto-Log Protocol" subsection inside quality_sentinel
    - Trigger A (FIX ATTEMPT LIMIT) calls `debt log` with severity gating and provenance
    - Trigger B (blocked gate) calls `debt log` in strict mode only with provenance
    - Cross-reference note in deviation_rules FIX ATTEMPT LIMIT section
    - Quality level gating: fast=skip, standard=critical+high, strict=all
    - Provenance fields: --source-phase and --source-plan always populated
  </done>
</task>

</tasks>

<verification>
1. `grep -c "Debt Auto-Log Protocol" /Users/tmac/.claude/agents/gsd-executor.md` returns >= 1
2. `grep -c "debt log" /Users/tmac/.claude/agents/gsd-executor.md` returns >= 2 (Trigger A and Trigger B)
3. `grep -c "logged-by executor" /Users/tmac/.claude/agents/gsd-executor.md` returns >= 2
4. `grep -c "source-phase" /Users/tmac/.claude/agents/gsd-executor.md` returns >= 2
5. `grep -c "source-plan" /Users/tmac/.claude/agents/gsd-executor.md` returns >= 2
6. `grep "Debt auto-log" /Users/tmac/.claude/agents/gsd-executor.md` shows cross-reference in deviation_rules
7. `node --test tests/*.test.cjs 2>&1 | tail -3` — no regressions (no .cjs changes)
</verification>

<success_criteria>
- Executor agent instructions include debt auto-logging at both hook points
- Quality level gating prevents logging in fast mode, limits to critical/high in standard
- Provenance fields (source_phase, source_plan) are always passed
- Existing sentinel logic unchanged — debt logging is additive only
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/milestones/v3.0/phases/3.4-agent-wiring/3.4-01-SUMMARY.md`
</output>
